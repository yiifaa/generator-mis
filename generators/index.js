let username
if(true) {
    const NAME = 'yiifaa'
    username = 'yiifaa'
    var age = 32
}

let pro = Promise.resolve({
    then (resolve, reject) {
        setTimeout(() => {
                resolve()
        })
    }
})

